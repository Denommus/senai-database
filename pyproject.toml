[project]
name = "senai"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "pandas",
  "numpy",
  "scikit-learn",
]

[project.scripts]
cli = "app.main:main"
